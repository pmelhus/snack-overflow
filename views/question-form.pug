extends layout.pug
include utils.pug

block content
  h1 Ask a question!
  div
    +validationErrorSummary
  div
    form(action='/questions' method='POST')
      div
        +field2('Title', 'title', "e.g. How many Oreos are too many Oreos?", 'text')
      div
        +textarea('Body', 'body')
      //- div
      //-   +select('Tags', 'tags', '', '' )
      div
        input(type='submit' value='Submit')
